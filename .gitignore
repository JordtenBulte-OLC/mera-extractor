# Compiled binary (Go sometimes leaves one in the working directory)
/mera-extractor
/extractor
build.log
.local
.ai-context

# Exclude the Mendix binaries as they are huge
/.mx-binaries
*.tar.gz

# Never — secrets, even though the current setup keeps them outside this repo entirely
*.env
.mera-secrets.sh
