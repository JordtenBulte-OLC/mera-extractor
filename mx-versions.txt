# Mendix versions to bundle into the extractor image, one per line.
# Each becomes a trimmed /opt/mx/<version>/mx via fetch-mx.sh
# add-trimmed-version, run per line by the Dockerfile's mx-fetch stage.
#
# Blank lines and #-comments are ignored. Keep this in sync with the
# /opt/mx/ version-matrix example in MERA-implementation-manual.md §1.3 —
# this file is meant to be the actual source of truth, that section is
# just illustrating the shape.
#
# Only 11.13.0 is real-diff-validated so far (see MERA-session-status.md —
# image diff + multi-commit diff both confirmed correct). Any other version
# added here will go through the same trial-trim candidate list
# (TRIM_CANDIDATES in fetch-mx.sh), which was built and validated against
# 11.13.0's file layout specifically — watch add-trimmed-version's own
# output for anything that looks off on a different version, and validate
# with a real mx diff (not just --help) before trusting it in production.

11.13.0
11.12.1
11.12.0
11.11.0
11.10.0